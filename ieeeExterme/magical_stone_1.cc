#include <iostream>

using namespace std;

int main()
{
    int n_status(0), q(0);

    cin >> n_status;
    int state_i[n_status];
    for (int i(0); i < n_status; i++)
    {
        cin >> state_i[i];
    }
    cin >> q;
    int k[q], times[q];
    for (int i(0); i < q; i++)
    {
        cin << k[i];
        cout << times;
    }

    return 0;
}