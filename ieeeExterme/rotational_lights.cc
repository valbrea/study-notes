#include <iostream>

using namespace std;

int main()
{
    int n_on(0), t_total(0);
    bool p_position[100000] = {0};

    cin >> n_on >> t_total;
    for (int i(0); i < n_on; i++)
    {
        cin >> p_position;
    }
    for(int i(0); i < t_total; i++)
    {
        if ()
        p_position++;
    }

    return 0;
}